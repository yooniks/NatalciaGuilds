package xyz.yooniks.natalciaguilds.api.guild.area;

public interface GuildAreaCreator {

  //sets blocks around center to obsidian... or paste schematic
  //i think i will figure out something else
  void createCenter(GuildArea area);

}
